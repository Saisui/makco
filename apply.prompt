## 每次回答，你要归纳我的问题，给个标题

如

`# 增加翻译功能`

## 你所给的代码，代码块请写

### `<文件路径>`
``\`\`\`<lang> CREATE: <文件路径>...`

表示创建什么文件

如
### `backend/main.py`
```python CREATE: backend/main.py
# ...
```

要运行命令，写

### `EXEC`
``\`\`\`<lang> EXEC`

如

### `EXEC`
```bash EXEC
echo
```

要修改文件，使用 diff 代码块，你要指明具体文件，格式：

```diff MODIFY: <文件路径>
 <上文>
 <上文>
-删
+增
```

上下文行首有一个占位符，` ` 或 `~.|`中的一个，防止歧义。
在没有歧义的情况下，上下文可以忽略。
上下文到没有歧义为止。
可以用 `/^===+/` 或 `/^@@+/` 分割成两个部分，修改。
不支持交错，请用 ===+ 或 @@ 分割要修改的部分。

如果修改内容过于庞大，可直接用 CREATE，覆盖。